# mongo-docker

MongoDB-branded Docker environments for demos and content creation.

> Stub — filled out in Wave 5 once CLI commands are stable.

## Install

```bash
npm install -g mongo-docker
```

## Usage

```bash
mongo-docker        # open TUI
mongo-docker list   # list available images
mongo-docker connect node-shell  # connect to a container
```
